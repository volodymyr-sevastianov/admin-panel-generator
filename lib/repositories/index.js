import { makeUniversalRepostitory } from "./repository";
import { makeMetaDataRepository } from "./meta-data-repository";

export default Object.freeze({
  makeUniversalRepostitory,
  makeMetaDataRepository
});
