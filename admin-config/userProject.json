{
  "columns": {
    "id": {
      "type": "string",
      "maxLength": 255,
      "constraint": "primary key"
    },
    "userId": {
      "type": "string",
      "maxLength": 255,
      "constraint": "foreign key"
    },
    "projectId": {
      "type": "string",
      "maxLength": 255,
      "constraint": "foreign key"
    }
  },
  "relationMappings": {
    "projectId": {
      "relation": "oneToMany",
      "join": {
        "from": "projectId",
        "to": "projects.id"
      }
    },
    "userId": {
      "relation": "oneToMany",
      "join": {
        "from": "userId",
        "to": "users.id"
      }
    }
  }
}