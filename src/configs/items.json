{
  "columns": {
    "id": {
      "type": "integer",
      "maxLength": null,
      "constraint": "primary key"
    },
    "created_at": {
      "type": "date",
      "maxLength": null,
      "constraint": null
    },
    "updated_at": {
      "type": "date",
      "maxLength": null,
      "constraint": null
    }
  },
  "relationMappings": {
    "users": {
      "relation": "manyToMany",
      "join": {
        "from": "items.id",
        "through": {
          "table": "users-items",
          "from": "users-items.item-id",
          "to": "users-items.user-id"
        },
        "to": "users.id",
        "toTable": "users"
      }
    }
  }
}