{
  "columns": {
    "id": {
      "type": "integer",
      "maxLength": null,
      "constraint": "primary key"
    },
    "make-id": {
      "type": "integer",
      "maxLength": null,
      "constraint": "foreign key"
    }
  },
  "relationMappings": {
    "make-id": {
      "relation": "oneToMany",
      "join": {
        "from": "make-id",
        "to": "makes.id"
      }
    }
  }
}